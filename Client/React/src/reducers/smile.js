import * as types from '../constants/ActionTypes';

const initialState = {

};

export default smile = (state = initialState, action) => {
  switch(action.type){
    case types.
    default: return state;
  }
}